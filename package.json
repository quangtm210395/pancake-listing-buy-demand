{
  "name": "muc",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0",
    "ethers": "^5.5.1"
  },
  "scripts": {
    "mainnet": "rm -f .env && cp .env.mainnet .env && node index.js",
    "testnet": "rm -f .env && cp .env.testnet .env && node index.js"
  }
}
